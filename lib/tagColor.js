const slugify = (str) => str.replace(/\s+/g, '-').toLowerCase();

const tags = {
  just: {
    name: 'just',
    bg: 'linear-gradient(90deg, #ffa0ae 0%, #aacaef 75%)'
  }
};

export default tags;
